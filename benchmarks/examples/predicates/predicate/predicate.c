// This file is automatically generated by PRECiSA 

#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include<stdbool.h>
#include"precisa_prelude.c"




/*@
axiomatic real_function_mul {
logic real mul (real X, real Y) =
(X * Y);
}
*/


/*@
axiomatic fp_function_mul {
logic double mul_fp (double X_double, double Y_double) =
Dmul(X_double, Y_double);
}
*/


/*@
assigns \nothing;

behavior structure:
ensures ((\is_finite(X_double) && \is_finite(Y_double))
         ==> ((\result == mul_fp(X_double, Y_double)))) ;
*/
double mul_fp (double X_double, double Y_double) {
  double res_double;
  res_double = (X_double * Y_double);
  return res_double;
}


/*@
assigns \nothing;
ensures \forall real X, real Y; ((((((-60000 <= X) && (X <= 60000)) && ((-10000 <= Y) && (Y <= 10000))) && ((\is_finite(X_double) && \is_finite(Y_double)) && (\abs(X_double - X) <= ulp_dp(X)/2 && \abs(Y_double - Y) <= ulp_dp(Y)/2)))
                                  ==> ((\abs((\result - mul(X, Y))) <= 0x1.4350000000001p-23)))) ;
*/
double mul_num (double X_double, double Y_double) {
  return mul_fp (X_double, Y_double);
}


/*@
axiomatic real_pred_positive {
logic boolean positive (real X, real Y) =
(mul(X, Y) > 0);
}
*/


/*@
axiomatic fp_pred_positive {
logic boolean positive_fp (double X_double, double Y_double) =
(mul_fp(X_double, Y_double) > (double) (0));
}
*/


/*@
requires (0 <= E_0_double) ;
assigns \nothing;

behavior structure:
ensures \forall real X, real Y; ((((\is_finite(X_double) && \is_finite(Y_double)) && \is_finite(E_0_double))
                                  ==> ((\result
                                        ==> ((positive(X, Y) && positive_fp(X_double, Y_double))))))) ;
*/
bool positive_tauplus_fp (double X_double, double Y_double, double E_0_double) {
  bool res;
  res = mul_fp (X_double, Y_double) > E_0_double;
  return res;
}


/*@
assigns \nothing;
ensures \forall real X, real Y; ((((((-60000 <= X) && (X <= 60000)) && ((-10000 <= Y) && (Y <= 10000))) && ((\is_finite(X_double) && \is_finite(Y_double)) && (\abs(X_double - X) <= ulp_dp(X)/2 && \abs(Y_double - Y) <= ulp_dp(Y)/2)))
                                  ==> ((\result
                                        ==> (positive(X, Y)))))) ;
*/
bool positive_tauplus_num (double X_double, double Y_double) {
  return positive_tauplus_fp (X_double, Y_double, 0x1.4350000000001p-23);
}




/*@
requires (0 <= E_0_double) ;
assigns \nothing;

behavior structure:
ensures \forall real X, real Y; ((((\is_finite(X_double) && \is_finite(Y_double)) && \is_finite(E_0_double))
                                  ==> ((\result
                                        ==> ((! (positive(X, Y)) && ! (positive_fp(X_double, Y_double)))))))) ;
*/
bool positive_tauminus_fp (double X_double, double Y_double, double E_0_double) {
  bool res;
  res = mul_fp (X_double, Y_double) <= - (E_0_double);
  return res;
}


/*@
assigns \nothing;
ensures \forall real X, real Y; ((((((-60000 <= X) && (X <= 60000)) && ((-10000 <= Y) && (Y <= 10000))) && ((\is_finite(X_double) && \is_finite(Y_double)) && (\abs(X_double - X) <= ulp_dp(X)/2 && \abs(Y_double - Y) <= ulp_dp(Y)/2)))
                                  ==> ((\result
                                        ==> (! (positive(X, Y))))))) ;
*/
bool positive_tauminus_num (double X_double, double Y_double) {
  return positive_tauminus_fp (X_double, Y_double, 0x1.4350000000001p-23);
}


/*@
axiomatic real_function_test {
logic integer test (real X, real Y) =
positive(X, Y) ? (1) : (2);
}
*/


/*@
axiomatic fp_function_test {
logic integer test_fp (double X_double, double Y_double) =
positive_fp(X_double, Y_double) ? (1) : (2);
}
*/


/*@ axiomatic test_trans {
predicate test_stable_paths (real X, real Y, double X_double, double Y_double) =
((! (positive(X, Y)) && (! (positive_fp(X_double, Y_double)) && (\true && \true))) || (positive(X, Y) && (positive_fp(X_double, Y_double) && (\true && \true))));
}
*/


/*@
requires (0 <= E_0_double) ;
assigns \nothing;

behavior structure:
ensures (((\is_finite(X_double) && \is_finite(Y_double)) && \is_finite(E_0_double))
         ==> ((\result.value == test_fp(X_double, Y_double)))) ;

behavior stable_paths:
ensures \forall real X, real Y; ((\abs(mul_fp(X_double, Y_double) - mul(X, Y)) <= E_0_double
                                  ==> (((((\is_finite(X_double) && \is_finite(Y_double)) && \is_finite(E_0_double)) && \result.isValid)
                                        ==> (test_stable_paths(X, Y, X_double, Y_double)))))) ;
*/
struct maybeInt test_fp (double X_double, double Y_double, double E_0_double) {
  struct maybeInt res;
  bool aux_0 = positive_tauplus_bool(X_double, Y_double, E_0_double);
  if (aux_0.value)
  { res = some(1);
  } else { bool aux_1 = positive_tauminus_bool(X_double, Y_double, E_0_double);
           if (aux_1.value)
           { res = some(2);
           } else { res = none();
           }
  }
  return res;
}


/*@
assigns \nothing;
ensures \forall real X, real Y; ((((((-60000 <= X) && (X <= 60000)) && ((-10000 <= Y) && (Y <= 10000))) && ((\result.isValid && (\is_finite(X_double) && \is_finite(Y_double))) && (\abs(X_double - X) <= ulp_dp(X)/2 && \abs(Y_double - Y) <= ulp_dp(Y)/2)))
                                  ==> ((\abs((\result.value - test(X, Y))) <= 0x0p+0)))) ;
*/
struct maybeInt test_num (double X_double, double Y_double) {
  return test_fp (X_double, Y_double, 0x1.4350000000001p-23);
}


int main () { return 0; }