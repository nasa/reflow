functioncall4: THEORY BEGIN


  f(V1, V2 : real): int
  = LET DET : real = (V1 - V2)IN
       IF DET <= 0 THEN  2
       ELSE             -2
       ENDIF

  g(X,Y: real) : bool
  = IF X<0 THEN
       X<0
     ELSE
       Y<0
     ENDIF




END functioncall4
