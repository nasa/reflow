// This file is automatically generated by PRECiSA 

#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include<stdbool.h>
#include"precisa_prelude.c"




/*@
axiomatic real_function_sgn {
logic integer sgn (real X) =
(X >= 0) ? (1) : (-1);
}
*/


/*@
axiomatic fp_function_sgn {
logic integer sgn_fp (double X_double) =
(X_double >= (double) (0)) ? (1) : (-1);
}
*/


/*@ axiomatic sgn_trans {
predicate sgn_stable_paths (real X, double X_double) =
((! ((X >= 0)) && (! ((X_double >= (double) (0))) && (\true && \true))) || ((X >= 0) && ((X_double >= (double) (0)) && (\true && \true))));
}
*/


/*@
requires (0 <= E_0_double) ;
assigns \nothing;

behavior structure:
ensures ((\is_finite(X_double) && \is_finite(E_0_double))
         ==> ((\result.value == sgn_fp(X_double)))) ;

behavior stable_paths:
ensures \forall real X; ((\abs(X_double - X) <= E_0_double
                          ==> ((((\is_finite(X_double) && \is_finite(E_0_double)) && \result.isValid)
                                ==> (sgn_stable_paths(X, X_double)))))) ;
*/
struct maybeInt sgn_fp (double X_double, double E_0_double) {
  struct maybeInt res;
  if (X_double >= E_0_double)
  { res = some(1);
  } else { if (X_double < - (E_0_double))
           { res = some(-1);
           } else { res = none();
           }
  }
  return res;
}


/*@
assigns \nothing;
ensures \forall real X; (((((-60000 <= X) && (X <= 60000)) && ((\result.isValid && \is_finite(X_double)) && \abs(X_double - X) <= ulp_dp(X)/2))
                          ==> ((\abs((\result.value - sgn(X))) <= 0x0p+0)))) ;
*/
struct maybeInt sgn_num (double X_double) {
  return sgn_fp (X_double, 0x1p-38);
}


int main () { return 0; }